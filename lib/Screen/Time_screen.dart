import 'package:flutter/material.dart';

class Time_screen extends StatelessWidget {
  const Time_screen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
