import 'package:flutter/material.dart';

class Constans extends StatelessWidget {
  const Constans({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
